# dashboards/

The deliverable HTML lives here.

`qatar-2022-dashboard.html` is a single self-contained page. Copy it from `outputs/qatar-2022-dashboard.html` if it isn't already in this folder — `push-to-github.ps1` does that automatically when it runs.
